import type { NextConfig } from "next";

// Configuração do Next.js
const nextConfig: NextConfig = {
  // Caso precise de configurações específicas depois, elas podem ser adicionadas aqui
};

export default nextConfig;